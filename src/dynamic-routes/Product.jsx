// Hooks Import:
import useAxios from "../hooks/useAxios"
import useScrollUp from "../hooks/useScrollUp"

// Styles Import:
import "../styles/global.scss"
import "../styles/page-styles/product.module.scss"

const Product = () => {
  return (
    <div>Product</div>
  )
}

export default Product